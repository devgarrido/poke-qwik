import { component$ } from "@builder.io/qwik";

export default component$(() => {
  return (
    <div>
      <h1 class="text-3xl">
        Esto es el contenido del dashboard Administrativo
      </h1>
      <p>Esta ruta debe ser privada</p>
    </div>
  );
});
